{
  "name": "website",
  "version": "1.0.0",
  "description": "Adam Torres personal website",
  "main": "index.js",
  "scripts": {
    "compile:sass": "node-sass sass/main.scss css/style.css",
    "devserver": "live-server",
    "start": "npm-run-all --parallel devserver watch:sass",
    "build": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "author": "Adam Torres",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^6.0.1"
  },
  "dependencies": {
    "live-server": "^1.2.1"
  }
}
